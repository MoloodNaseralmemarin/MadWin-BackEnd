@page
@using MadWin.Application.Services;
@inject IFactorService _factorService;
@inject IOrderService _orderService;
@inject IUserService _userService;

@model Shop2City.WebHost.Pages.Admin.IndexModel
@{
    ViewData["Title"] = "صفحه مدیریت";
    var countOrders = await _orderService.CountOrders();
    // var countfactors = await _factorService.CountFactors();
    // var countsmss = await _userService.CountSMSs();
    // var countusers = await _userService.CountUsers();
}

<div class="row mt-3 me-5 mb-3 ms-5">
    <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-3 mb-4">
        <div class="card shadow-sm bg-light border rounded p-3">
            <div class="card-body d-flex">
                <h6 class="mt-0 mb-5">تعداد کل سفارش‌ها</h6>
                <p class="badge text-bg-primary fs-6 ms-auto mt-5">@countOrders عدد</p>
            </div>
        </div>


    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-3 mb-4">
        <div class="card shadow-sm bg-light border rounded p-3">
            <div class="card-body d-flex">
                <h6 class="mt-0 mb-5">تعداد کل فاکتورها </h6>
                <p class="badge text-bg-primary fs-6 ms-auto mt-5">@countOrders عدد</p>
            </div>
        </div>
    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-3 mb-4">
        <div class="card shadow-sm bg-light border rounded p-3">
            <div class="card-body d-flex">
                <h6 class="mt-0 mb-5">تعداد کل پیامک های ارسالی </h6>
                <p class="badge text-bg-primary fs-6 ms-auto mt-5">@countOrders عدد</p>
            </div>
        </div>
    </div>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3 col-xl-3 mb-4">
        <div class="card shadow-sm bg-light border rounded p-3">
            <div class="card-body d-flex">
                <h6 class="mt-0 mb-5">تعداد مشتری های فعال </h6>
                <p class="badge text-bg-primary fs-6 ms-auto mt-5">@countOrders عدد</p>
            </div>
        </div>
    </div>
</div>